JNI是Java Native Interface
它提供了若干的API实现了Java和其他语言的通信（主要是C&C++）.这是百度百科上说的.通俗来说,就是JAVA调用C/C++函数的接口.如果你要想调用C系列的函数,你就必须遵守这样的约定.




海运段，沿江段，驳船段查询的功能有两个：
a. 根据海运段 前程拼接，后程拼接
b. 生成产品路径（ob+Trunk + ib），插入产品路径数据

ITM的查询
生成产品路径（ITM + ob + tk + ib + ITM）,插入产品路径数据
